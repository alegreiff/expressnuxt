{
  "name": "NUXTEXPRESS",
  "version": "1.0.0",
  "description": "Nuxt + express JS",
  "author": "Jaime de Greiff",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "express": "^4.15.3",
    "nuxt": "^1.0.0"
  },
  "devDependencies": {
    "cross-env": "^5.0.1",
    "node-sass": "^4.9.2",
    "nodemon": "^1.11.0",
    "sass-loader": "^7.1.0"
  }
}
